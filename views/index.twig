{% extends 'layout.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-12 mx-auto text-center">
                {% if user %}
                    <h1>
                        Welcome {{user.name}}
                    </h1>
                {% else %}
                    <b>Log In to see a surprise :-)</b>
                {% endif %}

            </div>
        </div>
    </div>
{% endblock %}